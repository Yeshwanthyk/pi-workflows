{
  "version": 1,
  "id": "basic-sequential",
  "workflow": {
    "script": "export const meta = { name: 'basic-sequential', description: 'Deterministic harness smoke', phases: [{ title: 'Run' }] }\nphase('Run')\nconst first = await agent('first synthetic task', { label: 'first' })\nconst second = await agent('second synthetic task', { label: 'second' })\nreturn { first, second }"
  },
  "runtime": {
    "hostCapacity": 4,
    "thinkingLevel": "medium"
  },
  "synthetic": {
    "delayMs": 2,
    "output": "synthetic-ok",
    "usage": {
      "input": 10,
      "output": 5,
      "cost": 0,
      "turns": 1
    }
  },
  "expected": {
    "status": "completed",
    "agentCount": 2
  },
  "benchmark": {
    "warmup": 1,
    "iterations": 5
  }
}
